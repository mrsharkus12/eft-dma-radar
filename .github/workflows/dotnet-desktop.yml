name: Deploy to GitHub Releases

on:
  push:
    branches:
      - beta
      - main
    paths:
      - 'resources/**'
      - 'src-silk/**'
      - 'src-arena/**'
      - 'src-wpf/**'
      - 'lib/**'
      - 'version.json'
      - '.github/workflows/**'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build & Deploy
    runs-on: windows-latest
    environment: production

    env:
      DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Get version info
        uses: dotnet/nbgv@master
        id: nbgv

      - name: Determine release channel
        id: channel
        shell: pwsh
        run: |
          $branch = "${{ github.ref_name }}"
          $baseVersion = "${{ steps.nbgv.outputs.SemVer2 }}"

          if ($branch -eq "beta") {
            $version = "$baseVersion-beta"
            $tag = "v$baseVersion-beta"
            $releaseName = "EFT $baseVersion-beta (Pre-release)"
            $prerelease = "true"
            $channel = "beta"
            $emoji = "🧪"
            $color = "16766720"
          } else {
            $version = "$baseVersion"
            $tag = "v$baseVersion"
            $releaseName = "EFT $baseVersion"
            $prerelease = "false"
            $channel = "stable"
            $emoji = "🚀"
            $color = "5763719"
          }

          "VERSION=$version" >> $env:GITHUB_OUTPUT
          "TAG=$tag" >> $env:GITHUB_OUTPUT
          "RELEASE_NAME=$releaseName" >> $env:GITHUB_OUTPUT
          "PRERELEASE=$prerelease" >> $env:GITHUB_OUTPUT
          "CHANNEL=$channel" >> $env:GITHUB_OUTPUT
          "EMOJI=$emoji" >> $env:GITHUB_OUTPUT
          "COLOR=$color" >> $env:GITHUB_OUTPUT

          Write-Host "Branch: $branch"
          Write-Host "Version: $version"
          Write-Host "Channel: $channel"
          Write-Host "Pre-release: $prerelease"

      - name: Send Discord Webhook (Build Started)
        if: env.DISCORD_WEBHOOK != ''
        shell: pwsh
        env:
          REPO: ${{ github.repository }}
          JOB_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          COMMIT_MESSAGE: ${{ github.event.head_commit.message }}
          CHANNEL: ${{ steps.channel.outputs.CHANNEL }}
          VERSION: ${{ steps.channel.outputs.VERSION }}
          EMOJI: ${{ steps.channel.outputs.EMOJI }}
          COLOR: ${{ steps.channel.outputs.COLOR }}
        run: |
          # Keep full commit message (title + description), cap at 1024 chars for Discord field limit
          $commitMsg = $env:COMMIT_MESSAGE.Trim()
          if ($commitMsg.Length -gt 1024) { $commitMsg = $commitMsg.Substring(0, 1021) + "..." }

          $payload = @{
            username = "GitHub Actions"
            embeds = @(
              @{
                title = "$env:EMOJI $env:REPO"
                description = "Build started — **$($env:CHANNEL.ToUpper())** channel"
                color = [int]$env:COLOR
                fields = @(
                  @{
                    name  = "Version"
                    value = "``$env:VERSION``"
                    inline = $true
                  },
                  @{
                    name  = "Channel"
                    value = $env:CHANNEL
                    inline = $true
                  },
                  @{
                    name  = "Commit"
                    value = $commitMsg
                  },
                  @{
                    name  = "Job Link"
                    value = $env:JOB_URL
                  }
                )
              }
            )
          }
          $json = $payload | ConvertTo-Json -Depth 5
          Invoke-RestMethod -Uri $env:DISCORD_WEBHOOK -Method Post -ContentType "application/json" -Body ([System.Text.Encoding]::UTF8.GetBytes($json))

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: "10.0.x"

      - name: Dotnet Publish (EFT)
        run: |
          dotnet publish src-silk/eft-dma-radar.csproj `
            -c Release `
            -r win-x64 `
            --self-contained false `
            /p:DebugType=None `
            /p:DebugSymbols=false `
            /p:Version=${{ steps.channel.outputs.VERSION }} `
            -o out/eft

      - name: Dotnet Publish (Arena)
        run: |
          dotnet publish src-arena/arena-dma-radar.csproj `
            -c Release `
            -r win-x64 `
            --self-contained false `
            /p:DebugType=None `
            /p:DebugSymbols=false `
            /p:Version=${{ steps.channel.outputs.VERSION }} `
            -o out/arena

      - name: Dotnet Publish (WPF)
        run: |
          dotnet publish src-wpf/eft-dma-radar-wpf.csproj `
            -c Release `
            -r win-x64 `
            --self-contained false `
            /p:DebugType=None `
            /p:DebugSymbols=false `
            /p:Version=${{ steps.channel.outputs.VERSION }} `
            -o out/wpf

      - name: Clean publish output
        shell: pwsh
        run: |
          Remove-Item -Path "out\eft\*.pdb"   -ErrorAction SilentlyContinue
          Remove-Item -Path "out\arena\*.pdb" -ErrorAction SilentlyContinue
          Remove-Item -Path "out\wpf\*.pdb"   -ErrorAction SilentlyContinue

      - name: Create zip archives
        shell: pwsh
        run: |
          Compress-Archive -Path "out\eft\*"   -DestinationPath "eft-dma-radar.zip"
          Compress-Archive -Path "out\arena\*" -DestinationPath "arena-dma-radar.zip"
          Compress-Archive -Path "out\wpf\*"   -DestinationPath "eft-dma-radar-wpf.zip"

      - name: Create GitHub Release
        shell: pwsh
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_TAG: ${{ steps.channel.outputs.TAG }}
          GH_RELEASE_NAME: ${{ steps.channel.outputs.RELEASE_NAME }}
          GH_PRERELEASE: ${{ steps.channel.outputs.PRERELEASE }}
          GH_SHA: ${{ github.sha }}
          GH_NOTES: ${{ github.event.head_commit.message }}
        run: |
          $flags = @(
            $env:GH_TAG
            "eft-dma-radar.zip"
            "arena-dma-radar.zip"
            "eft-dma-radar-wpf.zip"
            "--title", $env:GH_RELEASE_NAME
            "--target", $env:GH_SHA
            "--notes", $env:GH_NOTES
          )
          if ($env:GH_PRERELEASE -eq "true") {
            $flags += "--prerelease"
          } else {
            $flags += "--latest"
          }
          gh release create @flags

      - name: Send Discord Webhook (Release Published)
        if: env.DISCORD_WEBHOOK != ''
        shell: pwsh
        env:
          REPO: ${{ github.repository }}
          RELEASE_URL: https://github.com/${{ github.repository }}/releases/tag/${{ steps.channel.outputs.TAG }}
          RELEASE_NAME: ${{ steps.channel.outputs.RELEASE_NAME }}
          VERSION: ${{ steps.channel.outputs.VERSION }}
          CHANNEL: ${{ steps.channel.outputs.CHANNEL }}
          EMOJI: ${{ steps.channel.outputs.EMOJI }}
          COLOR: ${{ steps.channel.outputs.COLOR }}
        run: |
          $payload = @{
            username = "GitHub Actions"
            embeds = @(
              @{
                title = "$env:EMOJI $env:RELEASE_NAME"
                description = "✅ Release published!"
                url = $env:RELEASE_URL
                color = [int]$env:COLOR
                fields = @(
                  @{
                    name  = "Version"
                    value = "``$env:VERSION``"
                    inline = $true
                  },
                  @{
                    name  = "Channel"
                    value = $env:CHANNEL
                    inline = $true
                  },
                  @{
                    name  = "Download"
                    value = "[EFT]($env:RELEASE_URL) • [Arena]($env:RELEASE_URL) • [EFT-WPF]($env:RELEASE_URL)"
                  }
                )
              }
            )
          }
          $json = $payload | ConvertTo-Json -Depth 5
          Invoke-RestMethod -Uri $env:DISCORD_WEBHOOK -Method Post -ContentType "application/json" -Body ([System.Text.Encoding]::UTF8.GetBytes($json))
